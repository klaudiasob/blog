<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to octicon("home", 30), root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto"></ul>
<!--    <form class="form-inline my-2 my-lg-0" style="margin-right: 10px">-->
      <%#= form_tag({ controller: "houses", action: "index" }, { method: :get }) do %>
        <%#= label_tag(:query, "Search for:") %>
        <%#= text_field_tag(:query) %>
        <%#= submit_tag("Search") %>
      <%# end %>
    </form>
    <% if owner_signed_in? %>
      <form class="form-inline my-2 my-lg-0" style="margin-right: 10px">
        <%= link_to 'Nowy dom', new_house_path, class: 'btn btn-info' %>
      </form>
      <form class="form-inline my-2 my-lg-0" style="margin-right: 10px">
        <%= link_to 'Edytuj ustawienia konta', edit_owner_path(current_owner), class: 'btn btn-info' %>
      </form>
      <form class="form-inline my-2 my-lg-0" style="margin-right: 10px">
        <%= link_to 'Mailbox', conversations_path(current_owner), class: 'btn btn-info' %>
      </form>
      <form class="form-inline my-2 my-lg-0">
        <%= link_to 'Wyloguj', destroy_owner_session_path, method: :delete, class: 'btn btn-info' %>
      </form>
    <% else %>
      <form class="form-inline my-2 my-lg-0">
        <%= link_to 'Zaloguj', new_owner_session_path, class: 'btn btn-info' %>
      </form>
    <% end %>
  </div>
</nav>
